----Markdown 是一种轻量级标记语言，它允许人们使用易读易写的纯文本格式编写文档，然后转换成有效的 XHTML（或者 HTML）。
---

-VS Code 内置预览 Ctrl + Shift + V
. 标题 (Headings)
通过 # 号的数量来表示标题的级别。
语法：# 是一级标题，## 是二级，### 是三级，以此类推。
截图中对应：
阶段二：数据层与后端架构 (全栈核心) 前面有一个图标和加粗，这通常是 ## 或 ### 加上自定义图标。
. 数据库设计与连接 看起来像是一个列表项，但如果是大标题，通常是 ### 1. 数据库设计与连接。

---

-目前 这一行看起来像是分割线或者特殊的标题写法。
. 列表 (Lists)
用于罗列要点，分为有序列表和无序列表。
无序列表
语法：使用 -、+ 或 \* 加一个空格。
截图中对应：
markdown

编辑

- 关键点：在 tailwind.config.js 中...
- 目标：设计商品...
- 操作：推荐使用...
- 工具：使用 Prisma...
  (注意：截图中的圆点 o 可能是渲染后的样式，源码通常是 -)
  有序列表
  语法：使用数字加点 1. 加一个空格。
  截图中对应：
  markdown

编辑

. 数据库设计与连接
. 商品/内容展示系统
(Markdown 会自动处理序号，即使你全写 1.，渲染出来也会是 1, 2, 3...)
嵌套列表
语法：在子列表前增加缩进（通常是 2 个空格或 1 个 Tab）。
截图中对应：
markdown

编辑

. 数据库设计与连接

- 目标：...
- 操作：... - 子项... (如果有的话)
  . 行内代码 (Inline Code)
  用于标记代码片段、文件名或命令。
  语法：使用反引号 包裹内容。
  截图中对应：
  tailwind.config.js
  Prisma
  PostgreSQL
  module: "esnext"
  package.json
  lint-staged
  git commit
  . 强调文本 (Emphasis)
  加粗
  语法：使用两个星号 ** 或两个下划线 \_\_ 包裹。
  截图中对应：虽然截图中大部分是普通文本，但如果看到 “全栈核心” 或 “原子化” 这种加粗效果，源码就是 **全栈核心\*_。
  斜体
  语法：使用一个星号 _ 或一个下划线 \_ 包裹。
  . 引用块 (Blockquotes)
  用于引用一段文字，通常显示为左侧有竖线的灰色背景块。
  语法：在行首使用 >。
  截图中对应：截图中间那段长文本（“出现了数据库一直连不上...”）如果是在一个灰色框里，那就是引用块。
  markdown

编辑

> 出现了数据库一直连不上无法导入商品例子数据
> 安装 dotenv...
> . 分割线 (Horizontal Rules)
> 用于在视觉上分隔内容。
> 语法：三个或更多的 -、\* 或 \_。
> 截图中对应：
> ----目前 这一行。标准的分割线是 ---。截图里写的是 ----目前，这可能被渲染成了标题，或者前面的 ---- 被识别为分割线，后面的“目前”变成了紧接其后的文本（取决于具体的 Markdown 解析器）。标准的分割线写法是单独一行：
> markdown

编辑

---

. 链接 (Links)
虽然截图中没有明显的高亮链接，但 README 中常用。
语法：[显示文本](链接地址)
示例：[Neon.tech](https://neon.tech) 会显示为 Neon.tech。
. 任务列表 (Task Lists) - 推测
如果在列表项前有方框 [ ] 或 [x]。
语法：- [ ] 未完成的任务 或 - [x] 已完成的任务。
截图中对应：截图中的列表项前是圆点，不是方框，所以这里用的是普通列表。

在 GitHub、Gitee 等现代 Markdown 渲染器中，[!WARNING]、[!CAUTION]、[!TIP] 等标签的主要区别在于 视觉颜色、图标 以及 语义语气。它们用来帮助读者快速区分信息的紧急程度和类型。
以下是具体的对比：
| 标签 (Tag) | 渲染颜色 | 默认图标 | 语义/语气 | 适用场景 |
| :--- | :--- | :--- | :--- | :--- |
| [!NOTE] | 🔵 蓝色 | ℹ️ (信息) | 中性/提示 | 补充说明、背景信息、非关键的额外知识。 |
| [!TIP] | 🟢 绿色 | 💡 (灯泡) | 建议/技巧 | 最佳实践、快捷键、能提升效率的小技巧、解决方案。 |
| [!IMPORTANT] | 🟣 紫色 | ❗ (感叹号) | 重要强调 | 必须注意的关键点，忽略它可能导致功能不完整，但不会立刻报错。 |
| [!WARNING] | 🟠 橙色/黄色 | ⚠️ (警告三角) | 潜在风险 | 你遇到的情况。表示有问题发生了，需要小心，可能会导致构建失败或运行错误，但有解决办法。 |
| [!CAUTION] | 🔴 红色 | 🛑 (停止标志) | 严重危险 | 最高级别。表示如果不解决，会导致数据丢失、安全漏洞或系统完全崩溃。通常用于“切勿执行...”或“严重错误”。 |
